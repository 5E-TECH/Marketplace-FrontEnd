# Lockfile'dagi `resolved` manzillari bir xil bo'lishi uchun loyiha rasmiy
# registry'dan o'rnatiladi (global npmmirror sozlamasi bu yerda ishlamaydi).
# `npm audit` ham faqat shu registry'da ishlaydi.
registry=https://registry.npmjs.org/
